import { join } from 'path';

export const FOLDER_TOKEN = 'FOLDER_NAME';

export const PublicFolderPath = join(__dirname, '..', 'public');

export const allowedImageSize = 20971520;

export const locales = ['en', 'ar'];
